<!-- Shared logic: templates/shared/workflows/Refine_Prototype_Workflow_Shared_template.md -->

<!-- Strict mode: repo creation is a plain local `git init` only — no `gh repo create`, no remote. -->
<!-- Each iteration's commit stays local — never pushed; the user decides whether/when to push. -->
<!-- Drafted stories at loop end are written as local ST-XXXXXX.md files in the paired production repo via Create_Stories_Workflow.md's strict-mode path. -->
