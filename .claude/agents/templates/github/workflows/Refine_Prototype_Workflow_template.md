<!-- Shared logic: templates/shared/workflows/Refine_Prototype_Workflow_Shared_template.md -->

<!-- GitHub mode: repo creation uses `gh repo create` for a new `-ui-prototype` repo (never for an existing/reused one). -->
<!-- Each iteration's commit pushes live to the prototype repo immediately after committing (`git push`). -->
<!-- Drafted stories at loop end are created as GitHub Issues in the paired production repo via Create_Stories_Workflow.md's GitHub-mode path. -->
