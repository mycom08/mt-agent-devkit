# Technical Lead Memory

## Stored Facts

- **Version sequence (sprint 1):** `version.txt` is currently `0.0.9`. ST-000001 (retro marker story) claims `0.1.0`. ST-000005 (build software wiring) targets `0.1.1`. ST-000002/003/004 do not touch version.txt.
- **changes.json format note:** The `0.0.9` entry uses the newer object format (files + descriptions + checksums). Earlier entries (`0.0.1`–`0.0.7`) use the legacy flat array format. New entries should use the object format.
- **Stage 2 of Build_Software_Workflow:** orchestrator-direct action (no TL agent spawn). Orchestrator reads architecture.md and writes repo_structure.md directly.
- **Stage 3 of Build_Software_Workflow:** parallel general-purpose agents with inline instructions (not the devkit's own TL/PO agents). Split: one agent per output type across all repos.
- **Stage 4 of Build_Software_Workflow (init project):** orchestrator executes init-project-equivalent steps inline; does not call `init project` as a trigger; no --mode flag added to init project.
- **Story sequencing sprint 1:** ST-000002 first, then ST-000004 in parallel with ST-000002 (no shared files), then ST-000003 Stages 4–5 after ST-000004 merges.
- **Cross-project gh commands:** always use `--repo <slug>` explicitly — never rely on working directory git remote.
- **build software resume:** same trigger (`build software`) auto-detects state file at entry; Stage = last completed stage in state file; jump to Stage+1.
- **Retro marker placement:** `<!-- retro-adapted: preserve on sync -->` instruction belongs only in Stage 5 retro application step of Shared_Pipeline_Stages_template.md. Not on every stage.
- **Devkit Contribution privacy-scan source (ST-000011):** scan `.claude/agents/retros/sprint_N_summary.md` `### Findings` sections — NOT individual retro files (deleted at Batch Retro Review step 1e). Summary is the only durable, resumption-safe aggregate; resolve N from pipeline-state `Sprint` field.
- **Devkit Contribution access model (ST-000011):** authenticated path = issue-based (`gh issue create` on mycom08/mt-agent-devkit with ST-000010 export as body), uniform for all users (no push access / fork / write-detection needed). NOT PR/fork-based. Local-file fallback unchanged. AC reworded PR→Issue (PO owns the AC change).

- **status:testing canonical owner (ST-000012):** TL sets `status:testing` immediately after approving the PR and **before** merge. This is the canonical rule per `Technical_Lead_Rules_template.md §3` and `Shared_Pipeline_Stages_Shared_template.md` Stage 2 step 6. Implementer no longer sets this label.
- **Assignee disambiguation (ST-000012):** Two separate concepts in stories — the GitHub Issue Assignee (GitHub user account, sidebar, may be unset) vs the body `**Assigned:**` field (agent role, drives pipeline routing, mandatory). Story_Standard §2/§13 and Story_Standard_PO §2/§13 now include an explicit note.
- **Stage 4 next-story routing (ST-000012):** Stage 4 no longer promotes the next story to `in-progress`. Next-story routing is entirely Stage 0's responsibility (Story Discovery). This prevents double-routing in the sprint loop.
- **Template/working-mirror drift risk:** The working `Technical_Lead_Rules.md §3` had drifted from its template (said "after merge" vs template's "before merge"). Mirrors can drift silently between stories — verify both template and mirror when checking consistency.
- **Instruction-file path canonical structure (ST-000015):** Target-project instruction files belong at `.claude/agents/instructions/[role]_instructions.md` (subdirectory) — this is the CURRENT structure per `Update_Project_Workflow.md` detection (flat `.claude/agents/[role]_instructions.md` = OLD). As of PR #41 review, the deployed TEMPLATES still emit the flat path in **8 places**: `CLAUDE_Shared_template.md` roster (l.25-29) + integrity (l.43), the 5 `*_Rules_template.md` "Reference from:" (l.4 each), and `Create_Stories_Workflow_Shared_template.md` (l.8). A complete migration to the subdirectory requires editing these versioned templates → changes.json entry + version bump.
- **Path-move review pitfall (ST-000015):** When a story changes where a file is *written* (not its content), the fix must update EVERY referrer to the old location — grep the whole template+workflow corpus for the old path before approving. `validate_templates.py` does NOT check roster/rules ↔ write-location consistency, so a clean validator run gives false confidence. Also: if Update-side migration self-heals via a directory-existence check, moving Init to pre-create that dir silently disables the self-heal — the moved-to side must then own full scaffold consistency.
- **Scope-narrowed re-review outcome (ST-000015, PR #41 round 2):** RF-016 (instruction-path move) reverted out of PR #41 back to flat path, relocated to versioned story ST-000020 (Issue #42) — the correct fix requires editing the 8 flat-path referrer templates + a version bump, which is out of scope for an unversioned devkit-internal-fix story. PR #41 approved with scope narrowed to RF-015 + RF-017 only; revert verified residue-free via `git diff main...HEAD -- <file>` (fastest way to confirm a clean revert vs. trusting commit history). Stage 5 of Build_Software_Workflow.md intentionally has no completion-time `Stage:5` state write — it deletes the state file outright on success instead; do not flag this as a gap when reviewing state-write-timing fixes in that file.

- **Analyst Stage 2a target shape (sprint-4, ST-000022+ST-000024):** After both land, Stage 2a = a parallel wave {TL(opus) + PO(sonnet) + UI/UX Designer(sonnet, only if a repo has a UI layer)} spawned in one message, THEN QA(sonnet) sequenced after TL reports completion. TL writes `architecture.md` (incl. new **Testability Notes** section) and NO LONGER writes `testing_plan.md`; QA authors `testing_plan.md`; UI/UX Designer authors net-new `ui_design.md`. QA's spawn gate = TL completion ONLY — independent of the UI/UX Designer branch (`testing_plan.md` has no data dependency on `ui_design.md`). New state sessions: `qa_session` (ST-000024) + `uiux_session` (ST-000022). Both stories' Stage 2a/Stage 2d-table edits are additive → land ST-000024 (#65, foundational restructure, no dep) FIRST, then rebase ST-000022 (#63, gated on ST-000021) on top — no reconciliation round needed. Same assignee (Developer) owns both.
- **Analyst net-new doc `ui_design.md` (ST-000022):** UI/UX Designer-authored (conditional on UI layer). ST-000022 AC-2's "not by BA or PO as today" is a false premise — no wireframe/design doc exists in the pipeline today; it's a net-new doc. BA/PO reference it, never author it. Needs a `discussion.md` `## UI/UX Designer Questions/Suggestions` pair + a Stage 2d `ui_design.md → UI/UX Designer` feedback route.
- **Build Software CI-bootstrap decisions (ST-000025):** (1) The project-orchestrator folder is NEVER a `repo_structure.md` Stage 2 table row (it's created in Stage 4 Path B step 3, multi-repo only) — do not add a synthetic row (Stage 4 routes Path A/B by actual row count + scaffolds in table order). It's excluded from CI bootstrap by construction; drop it from AC-1's `none` examples (PO edit) and note the no-CI behavior in Stage 4 CI-Bootstrap prose only. (2) CI Bootstrap spawn pattern = **parallel-batched per wave** (mirrors Path B step d adaptive-tier spawn), runs after that wave's Java-skeleton step; targets are effectively the non-Java `full` repos (all in Wave 1); Path A degenerates to one spawn after step 6. Story is design-first (Priming §4) — Developer must draft CI taxonomy/job-mappings/prompt-shape as a story comment for TL approval before writing files.
- **ST-000025 design-review verdict — CHANGES REQUESTED (2026-07-20):** Developer's design draft (#66 comment) confirmed correct on 4/5 flagged points (ui-prototype→full, Tech-Stack-column-primary stack detection, none-repos-get-nothing, no new Entry-gate/state-field). **Required fix:** Path B placement of CI Bootstrap as a wave-closing step *after* the whole wave's `Scaffolded Repos` append breaks the documented Stage 4 resume invariant (line 52: append happens "before anything else... makes Stage 4 resumable from the state file alone"; resume rule at line 60 skips any appended repo with "no filesystem check needed") — a crash in that window permanently skips CI Bootstrap for the whole wave on resume. Fix options given: (a) run it per-repo inline between steps f/g (no resume-rule change), or (b) keep wave-batching but move the append to after the CI-Bootstrap batch + add an explicit resume-rule note for the new interrupted-window state. Also flagged: Path A step renumbering (7→8...11→12) leaves stale numeric self-references at lines 52/687 ("Path A step 9"/"Path A step 8") that must be updated in the same change. **General lesson:** when a new Stage-4 step is inserted, always check (i) the exact write-rule sentence in "Pipeline State" (not just the general Resume-rules prose) for a "before anything else" ordering guarantee it might violate, and (ii) grep the file for numeric step-number self-references that a renumber would strand.
- **ST-000025 round 2 — Design approved (2026-07-20):** Developer chose fix (b) (kept wave-batching; `Scaffolded Repos` append for Path B now happens for the whole wave, after that wave's new CI Bootstrap batch step, not per-repo-as-each-finishes-g) over fix (a), to preserve AC-3's already-agreed "parallel-batched per wave" wording. Closed the resume gap via an extension of the *existing* filesystem-fallback resume check (`git init`-initialized + `build_state.md` present) — not a new mechanism — covering the case where a wave crashes after all repos reach step g but before the wave's CI-Bootstrap-batch+append complete; on resume this re-enters the wave at the CI Bootstrap batch step, safe to re-run because that step's own per-repo guard (`full` + no `ci.yml`) skips already-bootstrapped repos. Verified independently against the live file (line numbers 52/60/682/687 all confirmed accurate; re-grepped for any other `Path A step`/`Path B step` self-references — none missed). Path A was already correctly placed in round 1 (CI Bootstrap precedes the done-flag), only its renumber needed patching. Story now clear for implementation.
- **ST-000025 PR #71 code review — APPROVED (2026-07-20):** Implementation matched the 2-round-approved design faithfully; resume-invariant fix (step h CI Bootstrap batch precedes step i wave append) verified in the actual shipped text, not just re-described. Confirmed 4 stale numeric cross-references fixed (lines ~52, ~60, ~682, ~687 pre-PR) via a full-file grep on the PR branch (not just the diff hunks) — 8 total `Path A step`/`Path B step` hits in the final file, all individually verified correct. AC-7 (no changes.json/version bump) confirmed via `git diff main...pr71 --stat`: exactly 4 files, none under `templates/`. **Reusable verification technique:** when a story renumbers/re-letters existing workflow steps, don't just check the diff hunks for missed cross-references — `git show <branch>:<file> | grep` the **final** file for the reference pattern, since a hunk-only check can miss a reference the PR correctly left untouched vs. one it should have touched but didn't (both look identical in a diff that shows no change there).

- **Roadmap-drain scoping rule (ST-000026, sprint-5):** The drain trigger is "a roadmap is authored/updated **in a context where a story tracker already exists**." `Analyst_Workflow.md`'s `implementation_roadmap.md` is permanently **out of scope** for direct drain — it's written to `/result/analyst/` inside the devkit itself, before any repo/tracker/story-counter exists, and its `[ST-XXXXXX]` IDs are placeholders that renumber on re-authoring. The drainable authoring points are (a) PO's in-project `docs/feature/<f>/plan/*Roadmap*.md` (so the obligation belongs in `Product_Owner_Rules_template.md`, not only in consumption workflows like Plan_Sprint/Create_Stories) and (b) Build Software, downstream. **Build Software split:** Stage 3's Agent A also emits `/result/build/<repo>/roadmap_stories_<repo>.md` (it already parses the stories, so this is free); the actual `gh issue create` drain runs as a new per-repo **Stage 5** step, after Doc Copy step 3 and before State File Cleanup — by then `gh repo create`/`gh project create`/`gh project link` are all done and Stage 5's resume rule is per-repo file-presence (safe re-run). **Do not put it in Stage 4** — that re-opens the `Scaffolded Repos` resume invariant ST-000025 just stabilised. Idempotency key = a `**Roadmap Source:** <file> :: Phase N :: <title>` marker line in the drained issue body, queried before create (never the placeholder ID).
- **Bug-story repro convention (ST-000027, sprint-5):** A `bug` label convention already exists (`Story_Standard_PO_template.md §13` labels list) — what's missing is a body field, not the label. Design: add a `## Reproduction` block (`**Repro Command:**`, `**Expected:**`, `**Actual:**`) to §13's bug-story variant. The pre-flight runs `Repro Command` **verbatim and nothing else** — synthesising a command from freeform AC prose is exactly the failure mode the story exists to prevent. Field absent or `unknown` ⇒ take the already-specified "no tooling" skip path ⇒ zero migration burden for bug issues authored before the change.
- **Pre-spawn eligibility filter vs. Blocked Story Procedure (ST-000027):** These are different classes and must branch differently. The Blocked Story Procedure stops the whole pipeline because an agent is *mid-story* with a live branch/PR — unsafe state to abandon. A **pre-spawn** filter (nothing spawned, no state) is structurally the same as `Sprint_Workflow`'s existing `Skip status:blocked stories` Pipeline Rule ⇒ skip the story and continue. Caveat that bit here: if the AC requires leaving the status label unchanged, skip-and-continue needs an **in-run skip list** (a `Repro Skipped:` field in `sprint_pipeline_state.md`), or the `status:ready` selection loop re-selects the same story forever. A fresh `continue sprint` run starts a new state file and re-attempts — which is correct, a human may have supplied repro steps meanwhile. `Start_Story_Workflow` targets one named story with no "next", so there the same condition means report-and-stop.
- **Devkit-internal vs. injected-template artifacts cannot share a source file (ST-000028):** `Build_Software_Workflow.md` / `Analyst_Workflow.md` live under `.claude/agents/workflows/` (devkit-internal, untracked in `changes.json`); anything under `.claude/agents/templates/` is injected into target projects and version-tracked. When a new *template* workflow needs behavior that a devkit-internal workflow already implements, the answer is **restate the convention + pointer**, never an extraction/refactor into a shared file. Corollary for `refine prototype`: reuse Stage 4 Path B's repo *conventions* (`<paired>-ui-prototype` naming, `scaffold_mechanical.sh`, lean 3-role adaptive tier, `UI_Prototype_Rules.md` as DoD) but **not** its "UI Prototype Scaffold Generation" agent — that agent reads `repo_structure.md` and hard-blocks when `ui_design.md` is missing (step 4 stop-on-blocker), neither of which exists outside a `build software` run.
- **Orchestrator state files live in the orchestrating workspace, not the managed repo (ST-000028):** `build_software_state.md` sits in the devkit's own `.claude/agents/tmp/` while tracking work done on *other* repos — that's the pattern to copy for any new loop/pipeline state file (`refine_prototype_state.md` → the target project's `.claude/agents/tmp/`). Three reasons that generalise: `.claude/agents/tmp/` is gitignored in both modes and already has a cleanup lifecycle; the managed repo is committed/pushed, so a state file there becomes committed noise inside the artifact under design; and the state file must exist *before* the managed repo does (create-path resumability). Bridge the two working directories by recording the managed repo's absolute path + per-step commit SHAs in the state file.
- **ST-000026 PR #85 review — CHANGES REQUESTED (2026-07-28):** Structure/scoping/dual-update/changes.json all correct first pass; all 4 CRs were mechanism-correctness, not design. (1) `Plan_Sprint` Stage 1 backstop queried `--label "status:backlog" --state open` — can't answer "is this tracked at all", so every drained-then-progressed story reads as drift. (2) `gh issue list --search "\"<marker>\" in:body"` is a **contiguous-token-subsequence phrase match**, not an exact-line match, and GitHub strips `**`/`::` at index time — a story whose title is a prefix of an already-drained story's title matches it and is silently skipped. Fix pattern: treat search as a *candidate set*, then confirm an exact full-line match against `--json number,body`. Also: GitHub's issue search index is eventually consistent (stale on immediate crash-resume). (3) Stage 5 resume gap, see next entry. (4) Stage 3 manifest `TBD` for `Assigned:` contradicts `Product_Owner_Rules §15` ("TBD is not permitted").
- **Warning-next-to-a-rule ≠ closing a resume gap (ST-000026, generalises ST-000025):** A resume rule is only fixed if the *instruction* changes. Adding "this check does not confirm step N ran" beside a rule that still says "skip the repo", plus an advisory "when in doubt, re-run it" elsewhere, leaves the gap fully intact — an orchestrator follows the numbered rule. When a new step is appended after the ones a file-presence resume check covers, the fix is to make that step **unconditional on resume** (safe whenever the step is idempotent), not to document the window.
- **Two reusable review checks for CLI-as-correctness-mechanism stories (ST-000026):** (a) When an AC's mechanism is a `gh`/CLI query, reason through one adversarial input against the tool's *documented matching semantics* — prose can be internally coherent while the shipped command answers a different question. (b) **Mode-parity is a cheap tell:** when a step has github/strict branches that answer different questions, the divergence localises the bug immediately. Also verify a TL design answer that leans on an existing resume/ordering rule by quoting the rule's actual skip condition — do not paraphrase it from memory (I did, and guaranteed a re-run behavior the file didn't provide).
- **ST-000026 PR #85 round 2 — APPROVED (2026-07-28):** All 4 CRs fixed as real instruction changes. CR-2 fixed at **5** call sites (I named 3) — implementer also caught that strict mode's plain substring grep carried the identical prefix-title risk; `grep -Fxq` is the correct strict-mode form (`-F` keeps `**` literal, `-x` forces whole-line). CR-3's fix is scoped entirely inside the Stage 5 resume bullet; verified the Stage 4 `Scaffolded Repos` bullet **byte-identical** between `main` and PR head via `diff <(git show main:file | grep ...) <(git show head:file | grep ...)`. Critically, the implementer also *removed* the now-obsolete advisory sentence from the Pipeline-Rules Resume note — leaving both would have quietly re-opened the gap. Residual non-blocking nits: `grep -Fxq` fails on CRLF-terminated lines; step 4b's `--limit 500` would false-negative into duplicates if ever truncated (safe by construction at Stage 5).
- **`gh issue list` vs `gh issue list --search` consistency (ST-000026):** plain `gh issue list --state all --limit N --json number,body` reads the **issues API** (immediately consistent); `--search` hits GitHub's **eventually-consistent search index**, so an issue created seconds earlier may not be returned. Where the result set is small (a freshly-scaffolded repo's tracker), prefer plain list + local exact-line match — it removes the index-lag hazard outright instead of documenting it, and sidesteps phrase-matching in the same step.
- **Cite sections, not line numbers (ST-000026, my error):** I wrote "`Product_Owner_Rules` §15" meaning line 15, which actually sits inside §1. The implementer propagated `§15` into 2 files; `validate_templates.py` caught it, costing a round-trip. When a rule is located via `grep -n`, resolve the line number to its `## N.` header before writing `§N` — a wrong-but-existing section number would ship silently.
- **Enumerating call sites in a change request invites treating the list as exhaustive (ST-000026):** phrase CRs as "at minimum these sites, plus any site sharing the same mechanism." I listed 3 and missed the strict-mode branch that had the identical defect.
- **TL/PO boundary on refinement answers:** Narrowing or clarifying an existing AC's technical meaning is TL's (per `Technical_Lead_Rules.md §2` AC-Clarifications), but **adding a standalone AC is PO scope** (`Story_Standard_TL.md §7` red flag: "TL commenting on scope"). When a design decision creates a genuinely new deliverable, fold the obligation into the existing AC's wording plus the **Technical Scope** section (TL-owned) and flag to PO that they may want it promoted to its own AC — don't write the AC yourself.

- **ST-000028 PR #87 review — CHANGES REQUESTED (2026-07-28):** Design/mode-split/state-file-location/ripple/bookkeeping all correct first pass; all 3 CRs sit in Step 1's repo-*creation* path. **CR-1 + CR-2 are one new failure class: an injected template (`templates/**`, runs in a target project) carrying executable references to devkit-only artifacts** — `bash .claude/agents/working/scripts/scaffold_mechanical.sh <devkit_root> …` (target projects have no `working/` dir and no `templates/` tree; `scaffold_mechanical.sh` deploys only `check_devkit_version.{ps1,sh}` into `scripts/`) and "follow `Build_Software_Workflow.md`'s Stage 4 Path B step d exactly (do not re-derive this list)" (that file is never deployed — `Build_Software_Project_Workflow.md` is a *different* file and only reaches the project-orchestrator folder). CR-2 is wording-only: the 8-file lean-roster list is restated inline and matches `Build_Software_Workflow.md:398` exactly. **CR-3:** new `refine_prototype_state.md` resume rule branches on empty-path→Step 1 and `Loop Status: active`→Step 3, but the write rule says "delete only after Step 4 — a resume during story drafting still needs the log"; `ended` matches no branch → falls into `active` → re-enters the iteration loop and drops the drafting handoff. Third story running with a resume-rule branch set lagging its own write rules (ST-000025 Stage 4, ST-000026 Stage 5).
- **Injected-template reference check (ST-000028, generalises the ST-000015 path-move lesson):** for any file under `.claude/agents/templates/`, every executable path and "read file X" pointer must resolve against the **deployed** inventory, not the devkit tree. The authority is already written down: `Sync_Devkit_Workflow_template.md` / `Update_Project_Workflow.md`'s "Expected files — `rules/` / `workflows/` / `scripts/`" lists. Fastest check: `grep -rn "agents/working/\|Build_Software_Workflow\|Analyst_Workflow\|Init_Project_Workflow" .claude/agents/templates/` — a hit outside a purely descriptive note is a defect. Distinct from a stale reference: the target exists, just not where the file will run, so it looks correct in the author's working tree.
- **`validate_templates.py` cannot see the above (ST-000028):** `_FILEREF_RE = re.compile(r"\.claude/agents/[\w./ -]+\.md")` matches `.md` only (script paths never examined), and `_resolve_file_ref` resolves against the **devkit repo root** + its `working/` mirror + `templates/<stem>_template.md` — so a devkit-only path inside an injected template resolves clean. A green validator run is not evidence that an injected template's references resolve in a target project. Same class as the ST-000015 fact that it does not check roster↔write-location consistency.
- **Split-workflow thin variants are comment-stripped before deployment (ST-000028):** `scaffold_mechanical.sh` (lines 91–92) drops every `<!-- … -->`-only line after line 1 of `templates/{mode}/workflows/X_template.md`, and appends a `---` + body only if non-comment content remains. Every current thin variant is comment-only ⇒ **github and strict deploy byte-identical files**. Any mode-dependent AC must therefore be satisfied by inline "**GitHub mode:** / **Strict mode:**" prose in the *shared* file; mode branching left in a thin variant ships as maintainer-only notes that never reach a target project. Verify by scaffolding both modes into scratch targets and diffing.
- **Scaffold-regression verification technique (ST-000028):** for any change to `scaffold_mechanical.sh` or the workflow/rules template set, run the script from the PR branch **and** from `origin/main` (via `git worktree add`) into two scratch targets and `diff -rq` the outputs. Converts several claims into evidence at once (no regression, correct file counts, both modes). Caveat: a `git worktree` checkout may materialise CRLF while the primary working tree is LF — an unrelated file showing a whole-file `1,Nc1,N` diff is that artifact, not a content change; confirm before chasing it.
- **Retro timing on a CHANGES-REQUESTED round (ST-000028):** `Retro_Rules.md` "When to Write" is unconditional — at the end of stage work, *before* reporting to the orchestrator — regardless of verdict. An orchestrator spawn prompt that says "if approved: … write your retro" is a simplification of the pipeline's happy path, not a narrowing of the rule; write the section either way and add addenda per round (matches ST-000015/25/26 precedent).

## Troubleshooting Facts

No troubleshooting facts recorded yet.
