# Technical Lead Memory

## Stored Facts

- **Version sequence (sprint 1):** `version.txt` is currently `0.0.9`. ST-000001 (retro marker story) claims `0.1.0`. ST-000005 (build software wiring) targets `0.1.1`. ST-000002/003/004 do not touch version.txt.
- **changes.json format note:** The `0.0.9` entry uses the newer object format (files + descriptions + checksums). Earlier entries (`0.0.1`–`0.0.7`) use the legacy flat array format. New entries should use the object format.
- **Stage 2 of Build_Software_Workflow:** orchestrator-direct action (no TL agent spawn). Orchestrator reads architecture.md and writes repo_structure.md directly.
- **Stage 3 of Build_Software_Workflow:** parallel general-purpose agents with inline instructions (not the devkit's own TL/PO agents). Split: one agent per output type across all repos.
- **Stage 4 of Build_Software_Workflow (init project):** orchestrator executes init-project-equivalent steps inline; does not call `init project` as a trigger; no --mode flag added to init project.
- **Story sequencing sprint 1:** ST-000002 first, then ST-000004 in parallel with ST-000002 (no shared files), then ST-000003 Stages 4–5 after ST-000004 merges.
- **Cross-project gh commands:** always use `--repo <slug>` explicitly — never rely on working directory git remote.
- **build software resume:** same trigger (`build software`) auto-detects state file at entry; Stage = last completed stage in state file; jump to Stage+1.
- **Retro marker placement:** `<!-- retro-adapted: preserve on sync -->` instruction belongs only in Stage 5 retro application step of Shared_Pipeline_Stages_template.md. Not on every stage.
- **Devkit Contribution privacy-scan source (ST-000011):** scan `.claude/agents/retros/sprint_N_summary.md` `### Findings` sections — NOT individual retro files (deleted at Batch Retro Review step 1e). Summary is the only durable, resumption-safe aggregate; resolve N from pipeline-state `Sprint` field.
- **Devkit Contribution access model (ST-000011):** authenticated path = issue-based (`gh issue create` on mycom08/mt-agent-devkit with ST-000010 export as body), uniform for all users (no push access / fork / write-detection needed). NOT PR/fork-based. Local-file fallback unchanged. AC reworded PR→Issue (PO owns the AC change).

- **status:testing canonical owner (ST-000012):** TL sets `status:testing` immediately after approving the PR and **before** merge. This is the canonical rule per `Technical_Lead_Rules_template.md §3` and `Shared_Pipeline_Stages_Shared_template.md` Stage 2 step 6. Implementer no longer sets this label.
- **Assignee disambiguation (ST-000012):** Two separate concepts in stories — the GitHub Issue Assignee (GitHub user account, sidebar, may be unset) vs the body `**Assigned:**` field (agent role, drives pipeline routing, mandatory). Story_Standard §2/§13 and Story_Standard_PO §2/§13 now include an explicit note.
- **Stage 4 next-story routing (ST-000012):** Stage 4 no longer promotes the next story to `in-progress`. Next-story routing is entirely Stage 0's responsibility (Story Discovery). This prevents double-routing in the sprint loop.
- **Template/working-mirror drift risk:** The working `Technical_Lead_Rules.md §3` had drifted from its template (said "after merge" vs template's "before merge"). Mirrors can drift silently between stories — verify both template and mirror when checking consistency.
- **Instruction-file path canonical structure (ST-000015):** Target-project instruction files belong at `.claude/agents/instructions/[role]_instructions.md` (subdirectory) — this is the CURRENT structure per `Update_Project_Workflow.md` detection (flat `.claude/agents/[role]_instructions.md` = OLD). As of PR #41 review, the deployed TEMPLATES still emit the flat path in **8 places**: `CLAUDE_Shared_template.md` roster (l.25-29) + integrity (l.43), the 5 `*_Rules_template.md` "Reference from:" (l.4 each), and `Create_Stories_Workflow_Shared_template.md` (l.8). A complete migration to the subdirectory requires editing these versioned templates → changes.json entry + version bump.
- **Path-move review pitfall (ST-000015):** When a story changes where a file is *written* (not its content), the fix must update EVERY referrer to the old location — grep the whole template+workflow corpus for the old path before approving. `validate_templates.py` does NOT check roster/rules ↔ write-location consistency, so a clean validator run gives false confidence. Also: if Update-side migration self-heals via a directory-existence check, moving Init to pre-create that dir silently disables the self-heal — the moved-to side must then own full scaffold consistency.
- **Scope-narrowed re-review outcome (ST-000015, PR #41 round 2):** RF-016 (instruction-path move) reverted out of PR #41 back to flat path, relocated to versioned story ST-000020 (Issue #42) — the correct fix requires editing the 8 flat-path referrer templates + a version bump, which is out of scope for an unversioned devkit-internal-fix story. PR #41 approved with scope narrowed to RF-015 + RF-017 only; revert verified residue-free via `git diff main...HEAD -- <file>` (fastest way to confirm a clean revert vs. trusting commit history). Stage 5 of Build_Software_Workflow.md intentionally has no completion-time `Stage:5` state write — it deletes the state file outright on success instead; do not flag this as a gap when reviewing state-write-timing fixes in that file.

- **Analyst Stage 2a target shape (sprint-4, ST-000022+ST-000024):** After both land, Stage 2a = a parallel wave {TL(opus) + PO(sonnet) + UI/UX Designer(sonnet, only if a repo has a UI layer)} spawned in one message, THEN QA(sonnet) sequenced after TL reports completion. TL writes `architecture.md` (incl. new **Testability Notes** section) and NO LONGER writes `testing_plan.md`; QA authors `testing_plan.md`; UI/UX Designer authors net-new `ui_design.md`. QA's spawn gate = TL completion ONLY — independent of the UI/UX Designer branch (`testing_plan.md` has no data dependency on `ui_design.md`). New state sessions: `qa_session` (ST-000024) + `uiux_session` (ST-000022). Both stories' Stage 2a/Stage 2d-table edits are additive → land ST-000024 (#65, foundational restructure, no dep) FIRST, then rebase ST-000022 (#63, gated on ST-000021) on top — no reconciliation round needed. Same assignee (Developer) owns both.
- **Analyst net-new doc `ui_design.md` (ST-000022):** UI/UX Designer-authored (conditional on UI layer). ST-000022 AC-2's "not by BA or PO as today" is a false premise — no wireframe/design doc exists in the pipeline today; it's a net-new doc. BA/PO reference it, never author it. Needs a `discussion.md` `## UI/UX Designer Questions/Suggestions` pair + a Stage 2d `ui_design.md → UI/UX Designer` feedback route.
- **Build Software CI-bootstrap decisions (ST-000025):** (1) The project-orchestrator folder is NEVER a `repo_structure.md` Stage 2 table row (it's created in Stage 4 Path B step 3, multi-repo only) — do not add a synthetic row (Stage 4 routes Path A/B by actual row count + scaffolds in table order). It's excluded from CI bootstrap by construction; drop it from AC-1's `none` examples (PO edit) and note the no-CI behavior in Stage 4 CI-Bootstrap prose only. (2) CI Bootstrap spawn pattern = **parallel-batched per wave** (mirrors Path B step d adaptive-tier spawn), runs after that wave's Java-skeleton step; targets are effectively the non-Java `full` repos (all in Wave 1); Path A degenerates to one spawn after step 6. Story is design-first (Priming §4) — Developer must draft CI taxonomy/job-mappings/prompt-shape as a story comment for TL approval before writing files.
- **ST-000025 design-review verdict — CHANGES REQUESTED (2026-07-20):** Developer's design draft (#66 comment) confirmed correct on 4/5 flagged points (ui-prototype→full, Tech-Stack-column-primary stack detection, none-repos-get-nothing, no new Entry-gate/state-field). **Required fix:** Path B placement of CI Bootstrap as a wave-closing step *after* the whole wave's `Scaffolded Repos` append breaks the documented Stage 4 resume invariant (line 52: append happens "before anything else... makes Stage 4 resumable from the state file alone"; resume rule at line 60 skips any appended repo with "no filesystem check needed") — a crash in that window permanently skips CI Bootstrap for the whole wave on resume. Fix options given: (a) run it per-repo inline between steps f/g (no resume-rule change), or (b) keep wave-batching but move the append to after the CI-Bootstrap batch + add an explicit resume-rule note for the new interrupted-window state. Also flagged: Path A step renumbering (7→8...11→12) leaves stale numeric self-references at lines 52/687 ("Path A step 9"/"Path A step 8") that must be updated in the same change. **General lesson:** when a new Stage-4 step is inserted, always check (i) the exact write-rule sentence in "Pipeline State" (not just the general Resume-rules prose) for a "before anything else" ordering guarantee it might violate, and (ii) grep the file for numeric step-number self-references that a renumber would strand.
- **ST-000025 round 2 — Design approved (2026-07-20):** Developer chose fix (b) (kept wave-batching; `Scaffolded Repos` append for Path B now happens for the whole wave, after that wave's new CI Bootstrap batch step, not per-repo-as-each-finishes-g) over fix (a), to preserve AC-3's already-agreed "parallel-batched per wave" wording. Closed the resume gap via an extension of the *existing* filesystem-fallback resume check (`git init`-initialized + `build_state.md` present) — not a new mechanism — covering the case where a wave crashes after all repos reach step g but before the wave's CI-Bootstrap-batch+append complete; on resume this re-enters the wave at the CI Bootstrap batch step, safe to re-run because that step's own per-repo guard (`full` + no `ci.yml`) skips already-bootstrapped repos. Verified independently against the live file (line numbers 52/60/682/687 all confirmed accurate; re-grepped for any other `Path A step`/`Path B step` self-references — none missed). Path A was already correctly placed in round 1 (CI Bootstrap precedes the done-flag), only its renumber needed patching. Story now clear for implementation.
- **ST-000025 PR #71 code review — APPROVED (2026-07-20):** Implementation matched the 2-round-approved design faithfully; resume-invariant fix (step h CI Bootstrap batch precedes step i wave append) verified in the actual shipped text, not just re-described. Confirmed 4 stale numeric cross-references fixed (lines ~52, ~60, ~682, ~687 pre-PR) via a full-file grep on the PR branch (not just the diff hunks) — 8 total `Path A step`/`Path B step` hits in the final file, all individually verified correct. AC-7 (no changes.json/version bump) confirmed via `git diff main...pr71 --stat`: exactly 4 files, none under `templates/`. **Reusable verification technique:** when a story renumbers/re-letters existing workflow steps, don't just check the diff hunks for missed cross-references — `git show <branch>:<file> | grep` the **final** file for the reference pattern, since a hunk-only check can miss a reference the PR correctly left untouched vs. one it should have touched but didn't (both look identical in a diff that shows no change there).

## Troubleshooting Facts

No troubleshooting facts recorded yet.
