<!-- Included by: templates/github/workflows/DoesNotExist_template.md -->

<!-- SHARED-START -->
# Fixture: invariant #3 -- unbalanced SHARED markers

This file is placed under scripts/test/fixtures/bad/shared/ so the validator
treats it as a shared file and applies check 3a (balanced SHARED markers).

The block below is intentionally missing its SHARED-END, which should be
flagged as unbalanced.
