# Fixture: invariant #4 -- retired trigger

This file is used by scripts/test/run.sh with:
  --test-retired-trigger TEST_RETIRED_TRIGGER_DO_NOT_USE

The token below must be flagged when that flag is passed.

Trigger: TEST_RETIRED_TRIGGER_DO_NOT_USE
