# Fixture: invariant #2 -- orphaned / unknown placeholder

This file contains an unknown double-brace placeholder token that is not
registered in the validator's KNOWN_DOUBLE_BRACE_TOKENS registry.

Value: {{UNKNOWN_TOKEN_XYZ_BAD_FIXTURE}}

It also has a malformed token (lowercase not matching [A-Z][A-Z0-9_]*):

Value: {{bad_token}}
