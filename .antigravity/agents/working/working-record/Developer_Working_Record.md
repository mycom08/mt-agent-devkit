# Developer Working Record

**Story:** ST-000038
**Completed:** Commenter gate for issue comments (source: Issue #106 + its follow-up closer-role comment). `Story_Standard_template.md` §12 gains a third gate ("Commenter — Before Posting an Issue Comment") whose load-bearing item is `wc -w` ≤ 200 — the same move `wc -c` made for memory files. §9 writing rules: one-topic-per-comment promoted out of the threading block to numbered rule 2 (with a carve-out that batching replies to questions asked together is permitted); rule 4 now prohibits inline command output/transcripts outright; new rule 5 "body edits are announced, not reproduced" (argument is staleness, not length). `Agent_Common_template.md` §2 rule 4 scoped to comments as well as memory writes — chose this over the §9 cross-reference branch the AC offered, since rule 4 is the rule's home. All four per-role views carry the delta, each phrased for its role's failure mode. Rules renumbered 1–9; grepped first — nothing in the corpus cites §9 rules by number. Scope addition found by grepping `150–200` rather than trusting the AC file list (Memory Fact 1): `Strict_Mode_Story_Guide_template.md` restates §9 verbatim for strict mode and would have drifted — same delta applied, no working mirror exists (§15 carve-out names this file). Working mirrors dual-updated per §15, treated as intentionally-diverged (devkit is GitHub-mode-only) so the delta was applied where it applies, not force-matched. Format skeleton untouched (AC7, verified via diff grep). `version.txt`→0.1.42, one `changes.json` entry (7 templates/ files). Validator: CI invocation passes clean; explicit-path run diffed against `main` shows zero new findings. PR #108, `status:review`.
**Round 2 (TL CHANGES REQUESTED on PR #108):** CR-1 — new §9 rule 4's absolute "never paste" contradicted §12's Reviewer gate, which mandates pasting `gh pr checks` output into a PR approval comment. Fixed with a **named carve-out**, not a scope-only qualification: §9 rule 4 now says it applies to issue *and* PR comments, then names one exemption — "gate-mandated CI evidence" — and declares it the only one. §12 Reviewer gate and §12 Commenter gate each cross-reference the carve-out from their own side (Commenter gate states it never applies to issue comments). Sites carrying the unqualified form, found by `grep -rn "never paste\|check transcripts"` over `.antigravity/`: cross-role source + mirror, Dev view + mirror, TL view + mirror, `Strict_Mode_Story_Guide_template.md` (no mirror), QA view + mirror. **QA view deliberately left unqualified** — §12's Reviewer gate applies to "Technical Lead, Developer peer review" only, so QA never posts a PR approval comment and cannot hit the conflict; adding an unreachable exemption would only weaken the rule for the role that pastes the most output. PO view has no transcript clause at all. Strict guide got the carve-out named as *inapplicable* (strict mode has no PRs) rather than importing PR vocabulary as a mandate. CR-2 — `version.txt` trailing newline restored (`printf '0.1.42\n'`). No version bump: 0.1.42 unmerged, so 4 `descriptions` entries in the existing `changes.json` 0.1.42 entry were reworded; no file added to `modified` (all 4 already listed). CHANGELOG `[Unreleased]` ST-000038 bullets amended in place. Validator CI invocation clean.
**In Progress:** Awaiting TL re-review on PR #108.
**Impediments:** None.

**Story:** ST-000037
**Completed:** New `Audit_Rules_template.md` (single `MA-n` finding class, report-only). `CLAUDE_Shared_template.md` Agent File Integrity audit carve-out (workflow, not a role). `Sync_Devkit_Workflow_template.md` + `Update_Project_Workflow.md` each gain a final "Stage 4 — Audit Pass": scoped to rules/instructions/CLAUDE.md writes, silent when clean, spawns fresh subagent, files an `audit:contribution` Issue, deletes the report. `working/workflows/Sync_Devkit_Workflow.md` mirror: rules-list bump only, Stage 4 deliberately not mirrored (that workflow never runs inside the devkit repo). Rules-file counts 9/19→10/20 across 3 files, 2 of which the AC didn't name (caught via Memory Fact 1). `version.txt`→0.1.41. Round-1 CR-1: `Refine_Prototype_Workflow_Shared_template.md` still listed 9 verbatim-tier files — fixed, `changes.json` gained a 4th entry. Merged (PR #104).
**In Progress:** None — story closed.
**Impediments:** None.

**Story:** ST-000035
**Completed:** New `working/rules/Audit_Rules.md` (Tier A spec: D-n/RP-n duplication, C-n report-only contradictions, X-n dead refs) and `workflows/Audit_Agent_Files_Workflow.md` (subagent spawn, crash-handling, dedicated-branch apply+revert). `CLAUDE.md` new section + command row, no roster entry. `validate_templates.py` `RUNTIME_PATH_PREFIXES` gains `.antigravity/agents/internal/` — caught a real CI-breaking reference locally before it reached a PR (Fix 1). No version bump (no `templates/` file touched). PR #103.
**In Progress:** None — story closed.
**Impediments:** None.

**Blockers & Watch-outs:**
- (none currently active)
