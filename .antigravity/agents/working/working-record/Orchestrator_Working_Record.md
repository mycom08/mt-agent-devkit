# Orchestrator Working Record

**Story:** ST-000037 — Inject Auditor into target projects — 2026-07-31
**Completed:** Full pipeline run (Start Story Workflow) — Dev implemented (PR #104: new `Audit_Rules_template.md`, audit carve-out in `CLAUDE_Shared_template.md`, detect-only Stage 4 wired into `Sync_Devkit_Workflow_template.md`+`Update_Project_Workflow.md`, `audit:contribution` label, `version.txt`→0.1.41); TL round 1 CHANGES REQUESTED (CR-1: missed 20th-rules-file ripple site in `Refine_Prototype_Workflow_Shared_template.md`, a deployed scaffold enumeration neither AC nor Dev's/TL's own ripple memory named), Dev fixed in 1 commit, TL round 2 approved; QA independently re-derived all 14 AC, zero regressions, ran the two-mode scaffold-output diff. Merged `0c7d144..9b6e891`, branch deleted. PO closed #102 `status:done`. Sprint retro appended to `sprint_6_summary.md`; user declined all 3 proposed retro actions (§15a enumeration-ripple checklist, TL reviewer grep-first checklist, TL Memory Fact 9 diff-recipe fix) this round — left as recorded observations. New this story: `Agent_Common.md §11` Token-Trace Log exercised for the first time — one file per agent under `.antigravity/agents/working/token-trace/ST-000037_<role>_steps_done.md`, self-estimates + orchestrator-filled real totals.
**In Progress:** — (closed)
**Impediments:** None.

**Story:** ST-000036 closure + ST-000037 revision & refine — 2026-07-31
**Completed:** Closed #101 (ST-000036) `status:done` — work was already merged (`7660c36`, 2026-07-30) but the issue was never closed; verified all 7 AC against actual commits before ticking, noted 2 wording deviations (AC4 "exits 0" → actual gate is differential no-new-`[ERROR]`; AC5 "version.txt bumped" → folded into `0.1.40` instead per the same change's own fold-in policy). Updated #102 (ST-000037) AC/Technical Scope: dropped the stale "Auditor agent" framing (`auditor_instructions_template.md`, roster entry) that ST-000035's live 2026-07-30 scope revision superseded. Then ran a single-story refine (Dev→TL→PO, not the other open sprint-6 backlog stories) on #102: Dev found the `Project_CLAUDE_template.md` carve-out factually wrong (that folder has no rules/instructions, `update project` never reaches it) and the "files the stage wrote" scope unstated/too broad; TL dropped the orchestrator folder from scope entirely and narrowed audit scope to files written via model-generated merge this run (`rules/`, `instructions/`, `CLAUDE.md` — verbatim-overwrite writes excluded, a target-side subagent has no template tree to diff against). PO applied TL's exact wording, sanity-passed the body, promoted #102 `status:backlog` → `status:ready`. Thread: https://github.com/mycom08/mt-agent-devkit/issues/102 (5 comments, resolved). #102 ready for pickup.
**In Progress:** — (closed)
**Impediments:** None.

**Blockers & Watch-outs:**
- `Agent_Common.md §11` (Token-Trace Log) and the matching `.gitignore` entry are edited locally on `main` but **not yet committed** — deliberately held per "only commit when asked." Both were carried across the ST-000037 branch switch via `git stash`/`pop` without incident.
- `Developer_Rules.md §6`'s docs-only-push skip-ci rule (`.antigravity/agents/**` = non-code) is mis-scoped for this repo — CI validates `.antigravity/agents/templates/**` and `.antigravity/agents/workflows/**`, the exact paths devkit stories live in. Already caused PR #99 to open with CI silently never running. Not yet fixed — all 3 auditor stories carry an explicit "do not skip CI" note in Technical Scope as a stopgap.
- Several retro-sourced improvement candidates remain unapplied across recent sprints (AC boilerplate impossible for split workflow files; structurally divergent §1 sections across roles; wrong `validate_templates.py` path in Developer's spawn prompt) — the §1 divergence item is now partly covered by ST-000036's audit pass.
